{
  "executors": {
    "cli": {
      "implementation": "../../../dist/tools/executors/cli/main",
      "schema": "./schema.json",
      "description": "Runs `echo` (to test executors out)."
    }
  }
}
